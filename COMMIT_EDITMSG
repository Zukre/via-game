VIA: реальное время (SSE push) — мгновенная синхронизация вместо опроса

- сервер: эндпоинт /events (SSE) + broadcast() на каждую мутацию данных
- клиент: EventSource как главный канал, опрос раз в 3с — страховка
- убирает задержку до 1с между действием игрока и его видимостью у всех

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
